{
  "name": "astronomy-site",
  "version": "2.0.0",
  "private": true,
  "type": "module",
  "description": "Astronomy site with a small Node server for real accounts (Beast still works without it — see server/README or server/index.js).",
  "engines": {
    "node": ">=22.13"
  },
  "scripts": {
    "start": "node server/index.js"
  },
  "dependencies": {},
  "devDependencies": {}
}
